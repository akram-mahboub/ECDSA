version: "3.8"
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
```

---

## Step 2: Update your .gitignore

```
backend/venv/
frontend/node_modules/
__pycache__/
.env